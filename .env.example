# Echo 🤖 Multi-agents AI Framework - Sample Environment File
# Copy to `.env` and adjust values as needed.

# --- Application ---
ECHO_APP_NAME="Echo 🤖 Multi-agents AI Framework"
ECHO_DEBUG=false

# --- LLM Provider Configuration ---
# One of: openai | azure-openai (alias: azure) | anthropic (alias: claude) | google (alias: gemini)
ECHO_DEFAULT_LLM_PROVIDER=openai
ECHO_DEFAULT_LLM_MODEL=gpt-4.1
ECHO_OPENAI_DEFAULT_MODEL=gpt-4.1
ECHO_AZURE_OPENAI_DEFAULT_MODEL=gpt-4.1
ECHO_ANTHROPIC_DEFAULT_MODEL=claude-sonnet-4-20250514
ECHO_GEMINI_DEFAULT_MODEL=gemini-2.5-flash

# --- API Keys (set the one(s) required by your provider) ---
ECHO_OPENAI_API_KEY=
ECHO_AZURE_OPENAI_API_KEY=
ECHO_ANTHROPIC_API_KEY=
ECHO_GOOGLE_API_KEY=

# --- Azure OpenAI (only if using azure-openai/azure) ---
ECHO_AZURE_OPENAI_ENDPOINT=
ECHO_AZURE_OPENAI_API_VERSION=2024-02-15-preview
ECHO_AZURE_OPENAI_DEPLOYMENT=


# --- Plugin Configuration ---
# Single directory path or JSON array of directories
# Example JSON array: ["./src/plugins","./custom_plugins","./third_party_plugins"]
# Default in code is ./src/plugins
ECHO_PLUGINS_DIR=./src/plugins
ECHO_ENABLE_GIT_PLUGINS=true
ECHO_ENABLE_PLUGIN_VALIDATION=true

# --- API Server ---
ECHO_API_HOST=0.0.0.0
ECHO_API_PORT=8000
ECHO_CORS_ORIGINS=["*"]

# --- Storage Backend Configuration ---
# Conversation Storage Backend Options:
# - "memory"      → InMemoryConversationRepository (fast, no persistence)
# - "postgresql"  → SQLAlchemy + PostgreSQL (production, 75% storage optimization)
ECHO_CONVERSATION_STORAGE_BACKEND=memory

# --- Database Configuration ---
# PostgreSQL Configuration (Primary Database)
# ECHO_POSTGRES_URL=postgresql+asyncpg://echo_user:secure_password@localhost:5432/echo_db
# ECHO_POSTGRES_POOL_SIZE=20
# ECHO_POSTGRES_MAX_OVERFLOW=30

# Redis Configuration (Session Store & Caching)
ECHO_REDIS_URL=
# ECHO_REDIS_POOL_SIZE=20



# --- Session Configuration ---
ECHO_SESSION_TIMEOUT=3600
ECHO_MAX_SESSION_HISTORY=100

# --- Processing Configuration ---
ECHO_MAX_AGENT_HOPS=25
ECHO_MAX_TOOL_HOPS=50
ECHO_GRAPH_RECURSION_LIMIT=100

# --- SDK Hybrid Plugin Discovery (optional; echo_sdk utilities) ---
# Enable directory-based plugin discovery via the SDK helpers
# ECHO_ENABLE_DIRECTORY_PLUGINS=true
# Comma/semicolon-separated list of absolute or relative directories
# ECHO_PLUGIN_DIRS=/abs/path/one,/abs/path/two
# Default directory used by SDK discovery if present
# ECHO_PLUGIN_DIR=./plugins


# ============================================================================
# QUICK START CONFIGURATIONS
# ============================================================================

# 🚀 DEVELOPMENT (No external dependencies)
# ECHO_CONVERSATION_STORAGE_BACKEND=memory
# ECHO_DEBUG=true

# 🏢 PRODUCTION (PostgreSQL) 
# ECHO_CONVERSATION_STORAGE_BACKEND=postgresql
# ECHO_POSTGRES_URL=postgresql+asyncpg://echo:password@localhost:5432/echo_prod
# ECHO_DEBUG=false


